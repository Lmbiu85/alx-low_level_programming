#include <stdlib.h>

#include <time.h>

#include <stdio.h>

/**
 * main - prints a text according number
 * Return: Always 0 (succes)
 *
 */

int main(void)
{
	int n;

	srand(time(00));
	n = rand() - RAND_MAX / 2;
	/* my code goes in there*/
	return (0);
}
